# Deep Echos

Deep Echos is a subterranean survival game about gathering crystals, managing supplies, fighting cave stalkers, and descending by lantern light.

To test locally, run:

```sh
python3 -m http.server --bind 0.0.0.0 8006
```

Then open http://localhost:8006.
